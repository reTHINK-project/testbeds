reThink-specific configurations are found below here.

./services/*.cfg       --- contains service definitions for the rethink testbed nodes
./templates/*.cfg      --- contains templates for rethink (hosts) etc.

./<NodeAcronym>/*.cfg  --- contains the spec of the specific rethink testbed node, i.e.
				the hosts to check for availablibility and running
				services.  
